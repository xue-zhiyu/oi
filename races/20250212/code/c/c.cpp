#include <cmath>
#include <iostream>
#define int long long

using namespace std;

constexpr int N = 510, M = 210;
int n, s[N], f[N][N][M];

signed main() {
	cout << ((double) (sizeof(f))) / 1024. / 1024. << "MB" << endl; 
	return 0;
}

